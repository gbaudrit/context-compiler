# Connectors.Git sample

Minimal sample showing how to use `ContextCompiler.Modules.Connectors.Git` to materialize a remote Git repository inside the workspace before compilation.

This example targets the `https://github.com/gbaudrit/context-compiler.wiki.git` wiki repository directly and limits ingestion to markdown files.
