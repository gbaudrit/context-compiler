# Output Contracts — Required Tests (Ultra)

1. All required artifacts exist after compile.
2. contractVersion present in JSON artifacts.
3. prompt.context.md includes mandatory sections in order.
4. Breaking change requires version bump (enforced by tests/CI policy).
