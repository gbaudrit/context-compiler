# Personas — Required Tests (Ultra)

## Unit tests
1. Persona resolution respects active order.
2. Missing personaId -> deterministic warning and skip.
3. mode append/prepend/replace produces correct final framing order.
4. Does not mutate IR.

## Integration tests
5. Applied personas list artifact matches config order.
