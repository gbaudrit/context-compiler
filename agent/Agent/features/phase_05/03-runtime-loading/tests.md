# Runtime Plugin Loading — Required Tests (Ultra)

1. Lock file with exact versions loads deterministically.
2. Hash mismatch -> deterministic failure.
3. Incompatible API version -> deterministic failure.
4. Loaded plugin discovered and registered in stable order.
