# Engineering Modules — Required Tests (Ultra)

## Unit tests
1. Ordering by priority then id is stable.
2. Module transformation is deterministic.
3. Modules preserve locators and evidence inputs.
4. Module failure stops pipeline (unless explicit skip policy exists later).

## Integration tests
5. Fixture with noisy text becomes normalized and yields stable fragments.
