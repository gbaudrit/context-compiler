# Security Guards — Required Tests (Ultra)

1. Detect known token patterns deterministically.
2. Redact token content and preserve EK.
3. Private key block triggers Critical+Block (exit code 2).
4. Injection pattern triggers configured action (quarantine/block).
5. Reports include stable ordering and no timestamps.
