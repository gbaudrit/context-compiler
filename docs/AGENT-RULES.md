# Agent Rules — Agent‑Max

These rules are mandatory.

- Documentation is authoritative
- Never invent behavior
- Never bypass guards
- Preserve Evidence IDs exactly
- Compiler never calls LLMs

If unsure, ask.
