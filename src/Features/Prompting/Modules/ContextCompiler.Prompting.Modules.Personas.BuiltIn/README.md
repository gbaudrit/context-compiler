# Built-in personas module

`ContextCompiler.Prompting.Modules.Personas.BuiltIn` is a ContextCompiler module.

ContextCompiler module that provides built-in personas.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Prompting.Modules.Personas.BuiltIn`.
