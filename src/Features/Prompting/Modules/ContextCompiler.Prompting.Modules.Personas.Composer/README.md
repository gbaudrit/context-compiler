# Personas composer module

`ContextCompiler.Prompting.Modules.Personas.Composer` is a ContextCompiler module.

ContextCompiler module that composes personas into the final prompt.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Prompting.Modules.Personas.Composer`.
