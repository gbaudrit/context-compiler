# Personas index module

`ContextCompiler.Prompting.Modules.Personas.Index` is a ContextCompiler module.

ContextCompiler module that produces an index of available personas.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Prompting.Modules.Personas.Index`.
