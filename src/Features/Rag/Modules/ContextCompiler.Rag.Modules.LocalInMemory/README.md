# Local RAG module

`ContextCompiler.Modules.Rag` is a ContextCompiler module.

ContextCompiler module that adds local RAG capabilities.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Rag`.
