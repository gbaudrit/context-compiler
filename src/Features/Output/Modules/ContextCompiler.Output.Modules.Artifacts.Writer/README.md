# Artifacts writer module

`ContextCompiler.Modules.Artifacts.Writer` is a ContextCompiler module.

ContextCompiler module that writes generated artifacts to disk.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Artifacts.Writer`.
