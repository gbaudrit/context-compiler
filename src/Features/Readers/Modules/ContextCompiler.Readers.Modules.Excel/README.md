# Excel reader module

`ContextCompiler.Readers.Modules.Excel` is a ContextCompiler module.

ContextCompiler reader module for Excel inputs.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Readers.Modules.Excel`.
