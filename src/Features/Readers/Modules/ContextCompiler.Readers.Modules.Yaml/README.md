# YAML reader module

`ContextCompiler.Readers.Modules.Yaml` is a ContextCompiler module.

ContextCompiler reader module for YAML inputs.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Readers.Modules.Yaml`.
