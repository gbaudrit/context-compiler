# Text reader module

`ContextCompiler.Readers.Modules.Text` is a ContextCompiler module.

ContextCompiler reader module for Text inputs.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Readers.Modules.Text`.
