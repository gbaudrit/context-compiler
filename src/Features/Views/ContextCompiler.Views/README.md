# Views module

`ContextCompiler.Modules.Views` is a ContextCompiler module.

ContextCompiler module that provides the core view capabilities.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Views`.
