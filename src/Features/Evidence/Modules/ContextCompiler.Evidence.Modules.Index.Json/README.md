# JSON evidence index module

`ContextCompiler.Evidence.Modules.Index.Json` is a ContextCompiler module.

ContextCompiler module that exports the evidence index in JSON format.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Evidence.Modules.Index.Json`.
