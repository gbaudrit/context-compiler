# JSON evidence graph module

`ContextCompiler.Evidence.Modules.Graph.Json` is a ContextCompiler module.

ContextCompiler module that exports the evidence graph in JSON format.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Evidence.Modules.Graph.Json`.
