# Health module

`ContextCompiler.Modules.Health` is a ContextCompiler module.

ContextCompiler module that produces context health metrics and diagnostics.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Health`.
