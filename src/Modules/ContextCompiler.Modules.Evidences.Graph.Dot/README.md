# DOT evidence graph module

`ContextCompiler.Modules.Evidences.Graph.Dot` is a ContextCompiler module.

ContextCompiler module that exports the evidence graph in DOT format.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Evidences.Graph.Dot`.
