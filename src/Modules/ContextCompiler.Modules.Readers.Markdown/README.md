# Markdown reader module

`ContextCompiler.Modules.Readers.Markdown` is a ContextCompiler module.

ContextCompiler reader module for Markdown inputs.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Readers.Markdown`.
