# Artifacts registry module

`ContextCompiler.Modules.Artifacts.Registry` is a ContextCompiler module.

ContextCompiler module that provides the artifacts registry.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Artifacts.Registry`.
