# Built-in modules bundle

`ContextCompiler.Modules.BuiltIn` is a ContextCompiler module.

ContextCompiler bundle module that groups built-in modules.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.BuiltIn`.
