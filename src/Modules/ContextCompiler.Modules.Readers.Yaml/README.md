# YAML reader module

`ContextCompiler.Modules.Readers.Yaml` is a ContextCompiler module.

ContextCompiler reader module for YAML inputs.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Readers.Yaml`.
