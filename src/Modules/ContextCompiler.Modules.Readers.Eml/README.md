# EML reader module

`ContextCompiler.Modules.Readers.Eml` is a ContextCompiler module.

ContextCompiler reader module for EML email sources.

## When to use

Use this module when you want to ingest `.eml` email messages and expose their headers, bodies, and attachments to the compilation pipeline.
