# Security module

`ContextCompiler.Modules.Security` is a ContextCompiler module.

ContextCompiler module that provides security checks and protections.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Security`.
