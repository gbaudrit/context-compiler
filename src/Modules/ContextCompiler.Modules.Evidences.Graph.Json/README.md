# JSON evidence graph module

`ContextCompiler.Modules.Evidences.Graph.Json` is a ContextCompiler module.

ContextCompiler module that exports the evidence graph in JSON format.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Evidences.Graph.Json`.
