# Copilot agent module

`ContextCompiler.Modules.BuiltIn.Agents.Copilot` is a ContextCompiler module.

ContextCompiler module with built-in Copilot agent integration assets.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.BuiltIn.Agents.Copilot`.
