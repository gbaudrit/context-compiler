# Email guard module

`ContextCompiler.Modules.Security.Guards.Email` is a ContextCompiler module.

ContextCompiler security guard module that detects and obfuscates email addresses in data parts.

## When to use

Use this module when you want to redact email addresses before downstream prompt composition or artifact generation.
