# JSON evidence index module

`ContextCompiler.Modules.Evidences.Index.Json` is a ContextCompiler module.

ContextCompiler module that exports the evidence index in JSON format.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Evidences.Index.Json`.
