# PDF reader module

`ContextCompiler.Modules.Readers.Pdf` is a ContextCompiler module.

ContextCompiler reader module for PDF inputs.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Readers.Pdf`.
