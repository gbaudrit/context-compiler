# Configuration module

`ContextCompiler.Modules.Configuration` is a ContextCompiler module.

ContextCompiler module that provides configuration capabilities and schema support.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Configuration`.
