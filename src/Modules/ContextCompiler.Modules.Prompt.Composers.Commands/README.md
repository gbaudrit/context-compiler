# Assumptions prompt composer module

`ContextCompiler.Modules.Prompt.Composers.Assumptions` is a ContextCompiler module.

ContextCompiler prompt composer module for the Assumptions section of the prompt.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Prompt.Composers.Assumptions`.
