# Constraints prompt composer module

`ContextCompiler.Modules.Prompt.Composers.Constraints` is a ContextCompiler module.

ContextCompiler prompt composer module for the Constraints section of the prompt.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Prompt.Composers.Constraints`.
