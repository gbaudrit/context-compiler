# Scriban prompt template module

`ContextCompiler.Modules.Prompt.Templates.Scriban` is a ContextCompiler module.

ContextCompiler module that renders prompts with Scriban templates.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Prompt.Templates.Scriban`.
