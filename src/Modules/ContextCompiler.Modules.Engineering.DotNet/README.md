# .NET engineering module

`ContextCompiler.Modules.Engineering.DotNet` is a ContextCompiler module.

ContextCompiler module with .NET engineering capabilities.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Engineering.DotNet`.
