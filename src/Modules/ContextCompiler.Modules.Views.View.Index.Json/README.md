# JSON view index module

`ContextCompiler.Modules.Views.View.Index.Json` is a ContextCompiler module.

ContextCompiler module that generates the JSON view index.

## When to use

Use this module when you want the capabilities provided by `ContextCompiler.Modules.Views.View.Index.Json`.
